#include "holberton.h"
#include <stdio.h>
/**
 * main - check the code for uppercase
 *
 * Return: 1 or 0
 */
int is_upper(int c)
{
if (isupper(c))
return (1);
else
return (0);
int main(void)
{
is_upper(int c);
return (0);
}
