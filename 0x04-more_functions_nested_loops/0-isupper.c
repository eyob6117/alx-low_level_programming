#include "holberton.h"
#include <stdio.h>
/**
 * main - check the code for uppercase
 *
 * Return: Always(0)
 */
int is_upper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{   
return (0);
}
