#include "holberton.h"
#include <stdio.h>
/**
 * _strcpy - function that copies the strin pointed to by src including
 *    including the terminatin null byte.
 * @dest: char pointer variable
 * @src: char pointer variable
 */
char *_strcpy(char *dest, char *src)
{
strcpy(dest,src);
return (dest);
}
