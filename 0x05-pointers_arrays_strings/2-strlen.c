#include "holberton.h"
#include <stdio.h>
/**
 * _strlen - a function that returns the length of a string
 * @s: character variable s
 *
 * Return: Always
 */
int _strlen(char *s)
{
int len = strlen(*s);
return (len);
}
