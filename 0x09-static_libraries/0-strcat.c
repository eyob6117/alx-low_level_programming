#include "holberton.h"
#include <stdio.h>
/**
 * *_strcat - function that concatenates two strings
 * @dest: pointer variable dest
 * @src: pointer variable src
 * Return: dest character value
 */

char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
